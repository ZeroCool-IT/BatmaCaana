CREATE TABLE Favorite (ID integer NOT NULL PRIMARY KEY, TYPE  integer NOT NULL, JSON  text NOT NULL);
CREATE TABLE Customers (ID integer NOT NULL PRIMARY KEY, UID integer NOT NULL UNIQUE, NAME text NOT NULL, AVATAR text NOT NULL);